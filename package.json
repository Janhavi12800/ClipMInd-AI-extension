{
  "name": "tradeprompt-ai",
  "version": "1.0.0",
  "private": true,
  "description": "TradePrompt AI — Smart Trading Prompts for Indian Markets",
  "scripts": {
    "setup": "cd backend && npm install && npm run setup:demo",
    "setup:full": "cd backend && npm install && npm run setup",
    "dev": "cd backend && npm install && npm run dev",
    "verify": "cd backend && npm run verify",
    "verify:youtube-prompt": "node scripts/validate-youtube-prompt.mjs",
    "sell:ready": "bash SELL_READY.sh",
    "sell:test": "node scripts/sell-ready-test.mjs",
    "go:live": "bash GO_LIVE.sh",
    "build:store": "bash scripts/build-store.sh",
    "build": "bash scripts/build-extension.sh",
    "assets": "python3 scripts/generate-store-assets.py",
    "start": "npm run setup && npm run dev"
  }
}
